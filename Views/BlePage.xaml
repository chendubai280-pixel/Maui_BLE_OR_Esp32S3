<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ble="clr-namespace:Plugin.BLE.Abstractions.Contracts;assembly=Plugin.BLE"
             x:Class="MauiApp2.Views.BlePage"  
             xmlns:viewmodels="clr-namespace:MauiApp2.ViewModels"
             Title="BlePage">

    <ContentPage.BindingContext>
        <viewmodels:MainViewModel/>
    </ContentPage.BindingContext>

    <!-- 修正 Grid 定义为 4 行 -->
    <Grid Padding="30,0" RowSpacing="20" RowDefinitions="8*, Auto, Auto, Auto">

        <!-- Row 0: 设备列表 -->
        <CollectionView
            Grid.Row="0"
            ItemsSource="{Binding DiscoveredDevices}"
            SelectedItem="{Binding SelectedDevice}"
            SelectionMode="Single">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="ble:IDevice">
                    <Grid Padding="10">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Label Text="{Binding Name}" FontSize="18" FontAttributes="Bold" />
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Row 1: 接收到的消息 (修正 Grid.Row) -->
        <Label 
            Grid.Row="1"
            Text="{Binding ReceivedMessage}"
            FontSize="20"
            HorizontalOptions="Center"
            VerticalOptions="Center"
            Margin="0, 20, 0, 0"/>

        <!-- Row 2: 状态文本 (修正 Grid.Row) -->
        <Label
            Grid.Row="2"
            VerticalTextAlignment="Center"
            Text="{Binding StatusText}" />

        <!-- Row 3: 按钮 (修正 Grid.Row) -->
        <Button
            Grid.Row="3"
            HorizontalOptions="Fill"
            Text="{Binding ButtonText}"
            Command="{Binding ActionCommand}"/>

    </Grid>
</ContentPage>
